# Image Pin Block 使い方

このページは、Image Pin Block を実際に使う方向けの詳しい使い方ガイドです。インストール手順や機能の一覧は [README.md](README.md) を、開発者向けの内部仕様は `docs/`(リポジトリのソースにのみ含まれます)を参照してください。

## 1. ブロックを追加する

投稿・固定ページの編集画面で、ブロック挿入メニュー(「+」)から「Image Pin Block」を検索して追加します。

## 2. 画像を選ぶ

ブロックを追加すると「画像を選択」ボタンが表示されます。メディアライブラリから画像(PNG または JPEG)を選ぶか、新しくアップロードしてください。

## 3. Fullscreen Editorを開く

画像を選択すると、通常のブロック上には**表示専用のプレビュー**が出るだけになります。ピンの追加・編集はすべて、画像をダブルクリックするか、右サイドバーの「ピンを編集」ボタンから開く**フルスクリーン編集画面(Fullscreen Editor)** の中で行います。

## 4. Pinを追加する

Fullscreen Editorを開いたら、Preview上で画像の何もない場所を**ダブルクリック**してください。「ここにピンを追加」というボタンが表示されるので、押すとその位置にピンが作成されます。

- 単にクリックしただけではピンは追加されません(誤操作防止のため)。
- Preview下の「ピン一覧」にある「＋」ボタンからも、画像中央へピンを追加できます。この「＋」ボタンは、ピンの数が多くタブが横スクロールしていても常に見える位置にあります。

## 5. Pinを選択・移動する

- ピンを**1回クリック**すると、選択状態になります(このときポップオーバーはまだ開きません)。
- **選択済みのピンをもう一度クリック**すると、そのピンのポップオーバー(説明のプレビュー)が開きます。さらにもう一度クリックすると閉じます。
- 別のピンをクリックすると、そちらが選択され、それまで開いていたポップオーバーは閉じます。
- ピンはドラッグで自由に移動できます。ドラッグ操作中はポップオーバーの開閉は変化しません。

## 6. Label

- **表示**: ラベルは、ピンの近くに常時表示される短い文字列です。画像マーカーの場合は「Show label」をONにしているときだけ表示されます。
- **入力**: Fullscreen Editor下部の「個別設定」の「ラベル」欄に入力します。
- **位置調整**: 画像上に表示されているラベル自体をドラッグすると、ピン/マーカーの周囲360度、好きな位置へ配置できます。
- ラベルをクリックした場合も、そのラベルが属するピンをクリックしたのと同じ扱いになります(非選択なら選択のみ、選択済みならポップオーバーをトグル)。

## 7. Description

- **入力**: 「個別設定」の「説明」欄に入力します。ここに入力した文章が、PCではポップオーバー、スマホでは画像直下の説明エリアに表示されます(画像上のラベルには表示されません)。
- **Popover**: PCでピンをクリック(設定によってはホバー)すると、説明文を表示する吹き出し(ポップオーバー)が画像の上に表示されます。
- **手動改行**: 「説明」欄で入力した改行は、ポップオーバー・説明エリアの表示にそのまま反映されます。

## 8. Marker Image

丸いピンの代わりに、任意の画像(アイコン等)をマーカーとして使えます。

- 「個別設定」からマーカー画像を選択・変更・解除できます。
- 拡大率は、数値入力またはPreview上でのドラッグリサイズ(選択中のみ表示されるハンドル)のどちらでも変更できます。どちらの操作も、もう一方へ即座に反映されます。
- マーカー画像は、常に本体画像の内側に収まるよう、移動・拡大の範囲が自動的に制限されます。
- 「Show label」で、そのマーカーにラベルを表示するかどうかを個別に切り替えられます。

## 9. Link / target動作

ピンをクリック・タップしたときの遷移先は、「個別設定」の「遷移先」で設定します。

- 同じページ内の見出しブロックをプルダウンから選ぶか、アンカー名を直接入力します。プルダウンに見出しを表示するには、その見出しブロック側であらかじめ「高度な設定」→「HTML アンカー」に文字列を設定しておく必要があります。
- 遷移先を設定していないピンは、クリックしても何も移動しません。

## 10. Zoom / Pan

- **Zoom UI**: Preview上部のスライダーで、100〜200%の範囲でZoomできます(100%が「全体表示」の状態です)。
- **マウスホイールでのZoom**: Preview上にマウスポインタがある状態でホイールを回すと、5%刻みでZoomを操作できます。
- **Pan**: 100%を超えてZoomしているとき、Preview上をドラッグすると表示位置を移動できます。
- **全体を表示する**: 現在のPreview寸法に対して、画像全体を収める倍率へ戻します。

> Fullscreen Editorのタッチ操作では、2本指によるPinch Zoomには対応していません。1本指でのPin/Marker/Label移動・Panのみに対応しています。

## 11. 画像として保存

「全体を表示する」の下にある「画像として保存」ボタンで、現在のピン配置を元画像の解像度でPNGとして書き出せます。

- 含まれるもの: 元画像、Pin、Marker画像、表示中のLabel
- 含まれないもの: ポップオーバー、選択枠やリサイズハンドルなどの編集UI、黒いletterbox部分
- 対応ブラウザでは「名前を付けて保存」ダイアログで保存先を選べます。対応していないブラウザでは、通常のダウンロードとして保存されます。

## 12. 編集を反映して終了

Fullscreen Editorのヘッダーにある「編集を反映して終了」(または右上の×)を押すと、編集画面が閉じます。

**重要**: このボタンは、編集した内容をブロックの属性(データ)へ反映するだけです。**投稿・固定ページ自体の保存にはなりません**。編集を終えたら、必ず投稿編集画面の「更新」「公開」「下書き保存」等でページ自体を保存してください。

## 13. スマートフォンでの表示

フロント(実際に公開されたページ)側でのスマホの動作は、ブロック全体の設定で以下から選べます。

- **タップで即遷移**: ピンをタップすると、説明を表示せず遷移先へ直接移動します
- **1回目タップで説明、2回目タップで遷移**: 1回目のタップで画像直下に説明エリアが開き、同じピンをもう一度タップすると遷移します
- **タップで説明表示、説明内のリンクで遷移**: タップで説明エリアが開き、説明文中のリンクから遷移します

説明文(Description)が空で遷移先だけが設定されているピンは、上記いずれの設定でも説明表示を省略し、直接遷移先へ移動します。

Fullscreen Editorはピンの編集作業をスマートフォンでも行えますが、PCでの利用を主な想定として作られています。スマートフォンでのピン編集そのものの使いやすさについては、まだ十分な実機確認ができていません。

## 14. よくある質問

**Q. 「編集を反映して終了」を押したのに、公開した記事に反映されません**
A. このボタンはブロックの属性へ反映するだけで、投稿自体の保存はしていません。Fullscreen Editorを閉じたあと、投稿編集画面の「更新」または「公開」ボタンを押してください。

**Q. ピンをクリックしても説明が出ません**
A. Fullscreen Editor内では、非選択のピンへの1回目のクリックは選択のみで、ポップオーバーは開きません。もう一度同じピンをクリックすると開きます。フロント側では、PC/スマホそれぞれの動作設定(ホバー・クリック・タップ)によって挙動が異なります。「ブロック全体の設定」で選んでいる動作を確認してください。

**Q. 見出しへジャンプする選択肢が候補に出てきません**
A. 遷移先にしたい見出しブロック側で、「高度な設定」→「HTML アンカー」が設定されているか確認してください。設定されていない見出しは候補に出てきません。アンカー名を直接入力する欄も使えます。

**Q. マーカー画像が大きくしても大きくなりません**
A. マーカー画像は、本体画像からはみ出さないよう自動的に拡大率の上限が制限されています。本体画像の表示サイズ自体を大きくすると、マーカーも合わせて大きく拡大できるようになります。
