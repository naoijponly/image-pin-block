# Changelog

このプロジェクトの変更点をまとめます。形式は [Keep a Changelog](https://keepachangelog.com/) に準じます。

## [0.3.0]

### 追加

- 吹き出しの先端のサイズを、Label・Popoverそれぞれ大・中・小の3段階から選べるようにした(吹き出しの先端を付けている場合のみ設定が表示される。既定は「中」で従来と同じ見た目。既存記事の保存データ・表示は変わらない)
- 画像マーカーのリサイズハンドルを、旧実装と同じ右下隅の青い丸に戻した(SVG移行後、Pin中心に無装飾の四角で表示されていた)

### 変更(内部実装。見た目・操作・保存データへの影響を目指さない大規模リファクタリング)

- Main Image・丸Pin・Marker・Label・吹き出し・PCポップオーバーの描画方式を、HTML/CSS/Canvasを個別に組み立てる方式から、単一のSVG Scene Rendererへ一本化した。通常のGutenbergプレビュー・フルスクリーン編集画面・フロント表示・拡大表示(Lightbox)・「画像として保存」(PNG)は、すべて同じ描画コードを共有する
- フルスクリーン編集画面の拡大表示(Lightbox)を、ページ全体を複製する方式から、同じデータを別のCamera(表示位置・倍率)で描き直す方式へ変更した。見た目・操作方法(ドラッグ移動・ホイール/ピンチでのズーム)は変更していない
- 「画像として保存」(PNG書き出し)を、Popover・選択枠等のPC用ポップオーバー・スマホの説明エリア・選択枠等の編集専用要素を含まない同じ描画結果から生成する方式に統一した
- 保存データ(ブロックの属性・ピンの位置や設定)の構造・意味は一切変更していない。既存記事の見た目・動作にも変更を意図していない
- Main Image・Pin・Marker・Label・Bubble・PopoverのSVG出力を直接確認するテストと、各表示経路やPNG書き出しへ別Rendererが再導入されることを検出する構造guardを追加した

### 修正

- ポップオーバーの背景・拡大表示の座標計算に、Preview領域とSceneの縦横比が異なる場合(letterbox発生時)にズレが生じ得た実装上の不具合を修正した(内部実装の変更に伴い発見・修正。旧実装ではこの不具合は発生しない構造だった)
- 上記のSVG Scene Renderer移行後、実際のWordPress環境での受入確認で見つかった以下の回帰を修正した。
  - 説明文(Description)の表示が、PC hover・クリック・フルスクリーン編集画面のいずれでも正しく動作しない(遷移先が未設定のPinではHover自体が無効化される、Pinから説明文本体へポインタを移動しただけで即座に閉じる、等)問題を修正した
  - ポップオーバーの文字サイズを大きくすると、日本語が約6文字ごとに強制的に折り返されてしまう問題を修正した(文字サイズに比例した幅で折り返す方式に変更)
  - 「画像として保存」で、同一サイト上の画像であっても保存に失敗する場合がある問題を修正した(HTTPS化前の記事に保存されたhttp://の画像URLを、HTTPSページ上で同一ホストの場合に限りhttps://へ読み替えるようにした。外部サイトの画像URLは対象外。保存データ自体は書き換えない)
- 説明文が画像高を超える長文ポップオーバーで、下部が表示領域外へ欠け、ホイールでスクロールできない問題を修正した

## [0.3.1]

### 修正

- 新規ブロックに画像を初めて選択したとき、通常のGutenbergブロック表示(モーダルを閉じた状態)が空白のままになる問題を修正した(画像未選択の間はキャンバス自体が存在しないため、画像選択前の1回だけ実行される初期化処理が、画像選択後に初めて現れるキャンバスを検出できていなかった。今回の主な原因)
- ブロックの通常表示(モーダルを閉じた状態)で、画像選択直後の一瞬、描画枠の高さが0になりブロックが消えたように見える問題を修正した(SVG描画が非同期で入るまでの高さを、判明していれば画像の縦横比、未確定ならmin-heightで確保する)
- メディアライブラリで画像を選び直す際、添付ファイルIDが不明(0)な場合に0を渡してしまい、選択・アップロードの反映が不安定になる場合がある問題を修正した
- 更新後にブラウザ・サーバーのキャッシュへ古いファイルが混ざっても気づけるようにした(全スクリプト・スタイルの版を1つにまとめ、どれか1つが更新されれば全て読み込み直される。古いファイルが混ざった場合は、エディタ上に案内を表示しConsoleにも原因を出力する)
- ブロック名が日本語環境で「画像ピンブロック」と翻訳表示されていたのを、他言語同様「Image Pin Block」の英語表記に戻した

## [0.2.0]

### 追加

- ピンの編集操作をすべて専用のフルスクリーン編集画面(Fullscreen Editor)に集約した。通常のGutenbergブロック表示は、編集操作を行わないプレビュー専用になった
- フルスクリーン編集画面のPreview上で、画像の空いている部分をダブルクリックすると「ここにピンを追加」の確認メニューが表示されるようにした(単クリックでは追加されない)。「＋」ボタンからも引き続き画像中央へ追加できる
- Label(ピンの近くに常時表示する短い文字列)をドラッグして、Pin/Marker周囲の好きな位置へ配置できるようにした
- 現在の状態を元画像の解像度で合成したPNG画像として保存できるようにした(「画像として保存」。対応ブラウザでは保存先を選択するダイアログを表示する)
- フルスクリーン編集画面のPreview上で、マウスホイールによるZoom操作(100〜200%)に対応した
- フルスクリーン編集画面に「編集を反映して終了」ボタンを追加した(右上の×と同じ終了処理。この操作は編集内容をブロックの属性へ反映するものであり、投稿自体の保存はWordPress本体の更新/公開/下書き保存で行う)

### 変更

- PC用ポップオーバー・スマホの説明エリアは、Description(説明文)のみを表示するようにした(画像上に表示されるLabelとの重複表示を解消。Labelは引き続き画像上に表示される)
- ピンの選択とポップオーバーの開閉を、別々の状態として扱うようにした。Pin/Marker/Labelは、1回目のクリックで選択のみ、選択済みのものを再度クリックするとポップオーバーが開閉する(ドラッグでは開閉しない)
- ピンを削除すると、作業を継続しやすいよう次のピン(無ければ前のピン、他に無ければ未選択)を自動的に選択するようにした
- 「画像として保存」ボタンを、フルスクリーン編集画面のヘッダーから「全体を表示する」の直下へ移動した

### 修正

- Descriptionが空で遷移先(target)が設定されているピンで、モバイルの「1回目タップで説明表示・2回目タップで遷移」「タップで説明表示・説明内のリンクで遷移」、PCの「クリックで説明表示・説明内のリンクで遷移」のいずれの設定でも、タップ/クリックしても遷移できなくなっていた問題を修正した

## [0.1.4]

### 変更

- ラベル未入力の丸マーカーピンで、画像上の可視ラベル・ポップオーバー・スマホの説明パネルに「Pin」の代替文字を表示しないようにした(画像マーカーの既存の扱いに統一。スクリーンリーダー向けの `aria-label` は従来どおり代替文字で補う)
- ラベル・説明文がどちらも未入力のピンでは、ポップオーバー・スマホの説明パネル自体を表示しないようにした(エディタのプレビュー・フロント表示の両方が対象)

### 修正

- 編集画面で、選択中のピンのポップオーバーのライブプレビューが、ブロックの選択が外れた後も表示され続ける問題を修正。他のブロックを選択したとき、画像の外をクリックしてブロックが非選択になったときに、プレビューが正しく消えるようにした
- 画像上の選択中のピン以外の場所をクリックしたときに、ピンの選択(ポップオーバーのプレビュー含む)が解除されるようにした
- 「ここにピンを追加」メニューが、ブロックエディタのキャンバス外(ページの余白など)をクリックしても閉じないことがある問題を修正

## [0.1.3]

### 追加

- ピンラベルの背景不透明度・縁取り(色・太さ)を設定できるようにした
- ポップオーバー(PC)・説明エリア(スマホ)の背景色・不透明度・文字色・縁取り(色・太さ)を設定できるようにした(未設定時は従来どおりの見た目を維持)
- 編集画面で、選択中のピンのポップオーバーを実画像の上に重ねてライブプレビュー表示するようにした

### 修正

- 上記のカラーピッカーで、ドラッグ中の色変更のたびに変更を記録しており、1回の操作でundo履歴(Ctrl+Z)が大量に積まれる問題を修正。変更の記録はドラッグを離した時点(またはテキスト入力欄の確定時)の1回のみに変更した
- 同じカラーピッカーで、グラデーション領域のドラッグ・クリック操作時に色の値が2値の間で往復し、選んだ色が確定しないことがある問題を修正。原因は `@wordpress/components` の `ColorPicker`(react-colorfulベース)内部の実装にあり、こちら側のコードで直接は修正できないため、色の確定タイミングを1回に絞ることで実質的に影響を受けない構成に変更した

## [0.1.2]

### 追加

- ラベルの文字サイズを設定できるようにした
- 選択中のマーカー画像ピンをドラッグしてリサイズできるようにした(丸マーカーは対象外)

### 変更

- ピンのサイズ・ラベルの文字サイズが、画像の表示幅に比例して拡縮するようにした(PCとスマホで見た目の比率が揃う)
- ピンのサイズ・ラベルの文字サイズの初期値を、画像選択時に元解像度から自動計算するようにした(それぞれ画像幅の2.5%・1.5%。既に設定済みの値は上書きしない)
- マーカー画像の表示幅に上限(本体画像幅の50%)を設けた

### 修正

- 数値入力欄(ピンのサイズ、マーカー画像の拡大率)で、入力途中の値が範囲外として弾かれ、既存の値を消して打ち直せない問題を修正。確定時(フォーカスが外れたとき/Enterキー)に範囲内へクランプするよう変更
- マーカー画像の当たり判定が見た目のサイズと一致しておらず、本体画像と同等以上の解像度のマーカー画像を指定すると画像のドラッグ操作や他のピンの操作が破綻する問題を修正
- マーカー画像が本体画像の外にはみ出さないよう、編集画面での移動・リサイズ操作の範囲を制限(丸マーカーは対象外)
- スマホで「1回目タップで説明表示、2回目タップで遷移」の設定にしている場合、説明エリアを閉じる処理とスクロールが同時に走ることでスクロール位置がずれる問題を修正

## [0.1.1]

### 修正

- スマホで拡大表示を開いた際、初期倍率が過大だった問題を修正。開いた直後は画像全体が画面に収まる倍率(fit-to-screen)を初期値とし、それを倍率の下限、元画像の解像度を上限とするよう変更

## [0.1.0] - 初期実装

### 追加

- Gutenberg カスタムブロック「Image Pin Block」(`image-pin-block/pins`)を追加
- 画像選択(メディアライブラリ、PNG / JPEG)
- 画像上の「何もない場所」をクリック →「ここにピンを追加」メニュー経由でのピン追加(誤操作防止のため即時追加はしない)
- ピンのドラッグによる位置調整(画像に対する%座標で保存)
- ピンごとの設定: ラベル、説明文、遷移先(同一ページ内の見出しブロックのプルダウン選択、またはアンカー名の直接入力)
- ピンごとのマーカー画像: 丸マーカーの代わりに任意の画像を指定可能。拡大率(元画像サイズ基準の%)とラベル表示の有無を個別設定
- ブロック単位の表示設定: PC での動作(`hover-click` / `click-link`)、スマホでの動作(`tap-jump` / `tap-tap` / `tap-link`)を独立して選択可能
- ブロック単位の見た目設定: ピンのサイズ・色(丸マーカーのみ)、ラベルの背景色・文字色(丸マーカー・画像マーカー共通)を WordPress 標準のカラーピッカーで設定可能
- フロント表示: PC はポップオーバー、スマホは画像直下の固定パネルで説明を表示。画像端に近いピンはポップオーバーの向きを自動調整
- 画像の拡大表示(自前実装、テーマの Lightbox 機能には非依存): ドラッグ / スワイプでの移動、マウスホイール / ピンチ / 画面上のボタンでの拡大縮小、閉じるボタン / Esc キー / 背景クリックでの終了
- 国際化対応: ソースコードの文字列は英語、日本語は `languages/image-pin-block-ja.*` の翻訳ファイルとして提供(編集画面・フロント側とも翻訳対象)

### セキュリティ

- フロント出力(`render_callback`)の全出力箇所にコンテキストに応じたエスケープ関数(`esc_html` / `esc_attr` / `esc_url` 等)を適用
- `pinColor` / `labelBackgroundColor` / `labelTextColor` を出力前に安全な CSS カラー表記(hex・rgb/rgba・hsl/hsla・CSS標準色名)かどうか検証し、不正な値は既定値にフォールバック(CSS インジェクション対策)
- ピン座標(x / y)を PHP 側でも 0〜100 にクランプ(編集画面側のクランプに依存しない)
- `pinSize`(4〜300px)・`markerScale`(1〜500%)に上限・下限を設け、範囲外の値は既定値にフォールバック
- ユーザーにより WordPress 公式 Plugin Check ツールのセキュリティ・一般カテゴリでエラー・警告ゼロを確認済み

### 既知の制限事項

- SVG 画像は非対応(PNG / JPEG のみ)
- 拡大表示の操作はマウス / タッチを前提とし、キーボードのみでの操作には非対応(Esc キーでの終了のみ対応)
- スマートフォン実機での動作確認は未実施
